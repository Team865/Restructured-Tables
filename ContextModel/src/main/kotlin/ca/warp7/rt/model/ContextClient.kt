package ca.warp7.rt.model

/**
 * Clients are the accessors of the computed data that present it to the user in some way.
 * A client component does not modify the underlying data structure but can build on top
 * of it in some way. Clients interact with the user and are allowed to save their
 * viewing configuration
 */
interface ContextClient {
}