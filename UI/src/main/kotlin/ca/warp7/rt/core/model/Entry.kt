package ca.warp7.rt.core.model

class Entry {
    companion object {
        val metricsSet: MutableSet<Metric<*>> =
                teamNumber_ / scout_ / driverStation_ / matchNumber_ / compLevel_ / event_ / year_ / dataSource_
    }
}